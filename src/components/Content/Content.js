import React, { Component } from "react";

export class Content extends React.Component {
    render() {
        return (
          <main>
              Content to do
          </main>
        );
    }
}